import logo from "./logo.svg";
import "./App.scss";

function App(data) {
  console.log(data);
  return (
    <div className="App">
      <header className="App-header">{String(null)}</header>
    </div>
  );
}

export default App;
